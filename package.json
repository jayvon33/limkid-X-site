{
  "name": "limkid-x-bot",
  "version": "1.0.0",
  "description": "Limkid-X WhatsApp bot pairing site",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@adiwajshing/baileys": "^5.0.0",
    "express": "^4.18.2",
    "qrcode": "^1.5.1"
  }
}          const interval = setInterval(async () => {
            const check = await fetch('/check-session');
            const status = await check.json();
            if(status.connected) {
              clearInterval(interval);
              statusDiv.textContent = '✅ WhatsApp paired successfully!';
              qrDiv.innerHTML = '';
            }
          }, 3000);
        } else {
          statusDiv.textContent = 'Error generating QR code';
        }
      } catch(err) {
        console.error(err);
        statusDiv.textContent = 'Something went wrong!';
      }
    });
  </script>
</body>
</html>
